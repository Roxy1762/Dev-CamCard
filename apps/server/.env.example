# 本地开发用 env（仅 apps/server 生效；docker 部署由根 .env + compose 注入）
DATABASE_URL="postgresql://camcard:camcard@localhost:5432/camcard"
PORT=2567
CLIENT_ORIGIN=http://localhost:5173,http://localhost:3000,http://localhost:3001
